/**
 * Service instance
 */
module.exports = class Service {

    /**
     * Initialize service
     * @param server Server instance
     */
    constructor(server) {
        this.server = server;
    }
}
